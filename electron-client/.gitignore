node_modules/
dist/
out/
*.log

# Game save data (created when running locally — written by spawned C# binary
# using CWD-relative paths until we fix the spawn working dir in main.js)
DATA/
SCORES/
logs/

# PixelLab job tracking files (temp, regenerated as needed)
assets/*_jobs.txt
assets/*_jobs2.txt

# API docs (downloaded on demand)
pixellab-api-docs.txt
